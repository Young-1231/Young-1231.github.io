# Navigation Links
#   To link a homepage section, use `/#section-id` (e.g. /#papers).
#   The weight parameter defines the order that the links will appear in.

main:
  - name: Bio
    url: /
    weight: 10
  - name: Experience
    url: experience/
    weight: 20
